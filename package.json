{
  "name": "portfolio-lab-spa",
  "version": "1.0.0",
  "private": true,
  "description": "SPA de gestion de portfolio en Vanilla JavaScript avec json-server.",
  "scripts": {
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
